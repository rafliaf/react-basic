// import { useCounter } from "../hooks/useCounter";

function Welcome() {
  // const { count, handleDecrement, handleIncrement, handleResetCount } = useCounter();

  return <h1>Component welcome to my React App</h1>;
}

export default Welcome;
